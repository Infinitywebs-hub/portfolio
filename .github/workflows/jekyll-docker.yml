name use a supported version of `actions/upload-artifact`.

Here is the updated code:

```yaml
- name: Upload Artifact
 : Jekyll Site CI/CD

on:
  push:
    branches:
      - uses: actions/upload-artifact@v4
  with:
    name: build
    path: _site
